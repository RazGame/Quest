.navbar-right(ng-controller="msNavBarLoginCtrl")
  form.navbar-form(ng-hide="identity.isAuthenticated()")
    ul.nav.navbar-nav
      li
        a(href="/signup") Регистрация
    .form-group
      input.form-control(placeholder="Почта", ng-model="username")
    .form-group
      input.form-control(type="password", placeholder="Пароль", ng-model="password")
    button.btn.btn-primary.btn-login(ng-click="signin(username, password)") Войти
  ul.nav.navbar-nav.navbar-right(ng-show="identity.isAuthenticated()")
    li.dropdown
      a.dropdown-toggle(href="", data-toggle="dropdown")
        | {{identity.currentUser.firstName + " " + identity.currentUser.lastName}}
        b.caret
      ul.dropdown-menu
        li(ng-show="identity.currentUser.isAdmin()")
          a(href="/admin/users") Все пользователи
        li(ng-show="identity.currentUser.isAdmin()")
          a(href="/admin/addGame") Создать игру
        li
          a(href="/profile") Профиль
        li
          a(href="", ng-click="signout()") Выйти